# Application data

Application databases in this directory are generated local artifacts and are never committed.

The Slack screenshot worker stores private evidence below
`data/app/slack_sales_evidence/` and recovery databases below `data/app/backups/`.
Those files, Slack payloads, and evaluation labels stay local and ignored. Only this
README is public.
